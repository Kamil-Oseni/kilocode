---
"raya": minor
---

Add five editor and canvas conveniences: press Cmd/Ctrl+I in a file to describe an inline edit for the current selection or line; review agent edits with Keep/Undo controls rendered next to each changed region in the gutter; a canvas Design Mode that highlights and picks an element to steer the agent; a canvas Capture button that saves a PNG of the live render; and an owner "lock a standard design system" setting that instructs agents to build UI against the approved system.
