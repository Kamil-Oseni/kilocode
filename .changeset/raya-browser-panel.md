---
"@kilocode/cli": minor
"kilo-code": minor
---

Add a persistent Playwright browser that agents can drive without per-action approval prompts while the same live session appears and accepts input inside a VS Code editor panel. Pace and serialize agent actions, retry transient failures with backoff, and support explicit manual takeover and resume.
