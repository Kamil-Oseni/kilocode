---
"@kilocode/cli": patch
---

Pause routines only after three consecutive runs block for the same reason. Keep routines awaiting a user answer enabled, and preserve successful runs as breaks in the failure sequence.
