---
"raya": patch
"@kilocode/cli": patch
---

Keep chat and in-editor Keep/Undo in sync across later edits, archive a completed goal so the next /goal can start, and force /canvas onto create_canvas instead of a standalone HTML file.
