// kilocode_change start - keep the shared compatibility import while diagnostics live behind profile admission
export { HeapSnapshot as Heap } from "@/kilocode/cli/heap-snapshot"
// kilocode_change end
