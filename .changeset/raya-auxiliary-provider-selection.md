---
"@kilocode/cli": patch
---

Keep auxiliary generation on the selected provider when no small model is available, while honoring explicit model overrides. Make saved goal-plan updates available throughout Auto's workflow.
