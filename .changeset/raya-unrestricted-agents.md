---
"@kilocode/cli": minor
---

Remove the read-only restriction on the Plan, Ask, Explore, and Orchestrator agents. They now have the same full tool access as the default agent — including bash, edit, write, delegation, and web search — while still honoring your own global and per-agent permission config.
