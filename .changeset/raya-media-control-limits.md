---
"raya": patch
---

Bound local media control requests to 1 MiB, reject trailing JSON before session side effects, and limit HTTP reads, writes and idle connections. Rebuild the media companion service to apply these limits.
