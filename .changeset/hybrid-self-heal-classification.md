---
"@kilocode/cli": minor
---

Self-heal repairs now sharpen their own triage: the repair agent reconciles the item's category, severity, approach, and title as its first step, and flags clear duplicates of existing open items. The fast keyword classifier stays as the instant, offline fallback, so intake is unchanged.
