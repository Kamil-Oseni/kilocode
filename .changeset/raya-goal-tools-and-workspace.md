---
"raya": minor
"@kilocode/cli": minor
---

<!-- raya_change -->

Keep goal work on tool-capable models, add named checkpoints, a global all-tools toggle, a goal audit log, and a themed Markdown preview.

Route goals intelligently while guaranteeing every orchestration turn and delegated subagent runs on a model that can call tools, transparently upgrading a no-tool model (such as DeepSeek chat) to the best available tool-capable one instead of stalling.

Save named workspace checkpoints and jump back to them from the Command Palette, turn on fully-autonomous agents everywhere with a single Grant All Tools setting, review why a blocked goal failed its completion audit from the goal card, and read Markdown previews in Instrument Serif and Outfit with Notion-like spacing. Default the Raya panel to the secondary sidebar.
