---
"kilo-code": patch
---

Fix Undo so Keep/Keep all sets a boundary — a later Undo only reverts edits made after the last Keep, instead of wiping work you already accepted. Reply in the language you are prompted in unless you ask otherwise. Reduce the in-editor browser's automated-Chrome fingerprint so fewer sites serve a bot wall.
