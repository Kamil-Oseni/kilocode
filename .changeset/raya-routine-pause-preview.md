---
"@kilocode/cli": patch
"raya": patch
---

Show a routine's effective failure pause even when saving its paused state needs recovery. Explain that enabling permits future runs, resets the consecutive-block count and retains earlier history.
