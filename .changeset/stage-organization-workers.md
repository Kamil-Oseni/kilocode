---
"raya": patch
---

Keep newly provisioned organization workers paused and bound to a durable staging receipt until their exact company graph is saved, then recover the complete unchanged group after interruption.
