---
"@kilocode/cli": patch
---

Support structured routine deliverables and acceptance criteria through the API and scheduling tool. Preserve each run's output requirements in its startup snapshot and include verification instructions in its goal.
