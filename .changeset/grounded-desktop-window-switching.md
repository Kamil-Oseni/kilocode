---
"@kilocode/cli": minor
"@kilocode/sdk": minor
"raya": minor
---

Let Raya list visible Windows application windows, switch to one exact freshly observed target, keep visible captured pixels and native input in one physical coordinate space across mixed-DPI, partially off-screen and oversized windows, bound capture memory and encoded output, refuse points covered by another application or actions while manual input is held, dispatch pointer gestures, Unicode text and two-axis scrolling as complete native batches, and stop safely when an action's outcome cannot be confirmed.
